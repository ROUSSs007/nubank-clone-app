import 'package:flutter/material.dart';
import 'package:intl/intl.dart';

const primaryColor = Color(0xFF830AD1);
const accentColor = Color(0xFF9C03FD);
const greyColor = Color(0xFFF0F1F5);

const double defaultPadding = 24;
const double defaultRadius = 12;

final currencyFormat = NumberFormat.currency(locale: 'pt_BR');
